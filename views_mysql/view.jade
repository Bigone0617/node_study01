html
    head
        meta(charset='utf-8')
        title Index
    body
        a(href='/topic') 
            h1 Server Side Javascript 
        
        ul 
            -each topic in topics 
                li 
                    
                    a(href='/topic/'+topic.id)= topic.title

        article
            if topic
                h2= topic.title 
                = topic.decription
                div= 'by ' + topic.author
            else 
                h2 Welcome 
                | This is Server side javascript tutorial.
        ul 
            if topic 
                li 
                    a(href='/topic/add/'+topic.id) EDIT
                li
                    a(href='/topic/delete/'+topic.id) DELETE
            

        p 
            button(onclick='location.href="/topic/add"') Create New
